-- Plugin: vim-surround
-- Description: Add, delete, change surrounding characters (quotes, brackets, tags) around text.
-- Keybindings: ys (add surround), ds (delete surround), cs (change surround)
--              Example: ysiw" (surround word with quotes), ds" (delete quotes), cs"' (change " to ')

return {
  "tpope/vim-surround",
}
